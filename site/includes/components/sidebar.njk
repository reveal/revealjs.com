<div class="sidebar fixed inset-0 pt-16 h-full z-90 w-full border-b -mb-16 lg:-mb-0 lg:static lg:h-auto lg:overflow-y-visible lg:border-b-0 lg:pt-0 lg:w-1/4 lg:block lg:border-0 xl:w-1/5">
	<nav>
		<ul>
			{% for item in navigation.main %}

			<li class="level-1 mb-2 md:mr-4">

			    <a href="{{ item.url | safe }}" class="link font-semibold text-gray-500 hover:text-gray-700 active:text-gray-700 {% if page.url == item.url %}active text-gray-700{% endif %}
			    "{% if page.url == item.url %} aria-current="page"{% endif %}>{{ item.label }}</a>

			</li>

			{% endfor %}
		</ul>
	</nav>
</div>